# Drivacy 프로젝트 방향성

마지막 업데이트: 2026-09-20 (KST)

이 문서는 팀과 개발 도구가 공유하는 현재 방향의 기준이다. 실제 구현·검증 결과는 README와 코드가 증명하며, 이 문서의 계획을 구현 완료로 표현하지 않는다.

## 프로젝트와 해커톤

Drivacy는 가입자의 상세 주행기록을 보험사에 제공하지 않고, 보험사가 승인한 안전운전 할인 조건의 충족 여부를 Midnight의 영지식증명으로 검증하는 데모를 목표로 한다. 대상은 Midnight Korea Hackathon 2026이며, 공식 프로그램 페이지의 제출 마감은 **2026-09-28 00:00 KST**다. 팀은 Luma 참가 등록을 완료했다.

- 공개 저장소: https://github.com/0xyuanyx/Midnight-Drivacy
- 해커톤 안내: https://www.hackathon.midnightkorea.org/kor
- 심사에서 확인하는 것: 저장소 클론과 컴파일, 제출 폼과 README의 일치, Midnight 기능의 명확성, 접근 가능한 데모.
- 제출에 필요한 내용: 프로젝트명, 한 줄 설명, README가 있는 공개 저장소, 실행 또는 데모 흐름, Midnight 구현 포인트. 데모 영상은 선택 항목이다.
- 팀 구성의 최신 확인: 개발자는 총 3명으로 프론트+백엔드 1명(백엔드 비중 적게), 백엔드 위주 2명이다. 기능 분담은 A·B·C로 표기하며 실명 연결은 미정이다. 이전의 기획 겸 개발·프론트엔드 겸 백엔드·백엔드 구분보다 이번 개발 인원 확인을 우선한다. 디자인 역할은 기존 기록에 1명으로 남아 있으며 [기능 분담안·공통 작업 원칙](docs/TEAM_WORKING_PLAN.md)을 참고한다.

## 확정된 MVP 방향

### 첫 수직 기능 Shared 계약 — 2026-09-17 확정

- `packages/shared`는 첫 수직 기능에서 공통으로 쓰는 `Role`, `User`, `Consent`, `InsuranceContract`, `SpecialContract`, `ApiError`, `RequestId`의 Zod 스키마와 TypeScript 타입을 public export로 제공한다.
- 역할은 `DRIVER`, `INSURER` 두 종류다. Shared `User`는 애플리케이션 경계에 필요한 ID·이메일·역할만 표현하며 Supabase Auth 전체 객체를 노출하지 않는다.
- Consent는 동의 여부와 동의 시각만 표현한다. 동의 종류와 버전은 근거 자료에서 확정되지 않아 이번 계약에 포함하지 않는다.
- 보험계약은 소유 사용자, 보험사, 보험기간, 상태와 연결 가능한 특약 목록을 표현한다. 특약은 계약 연결, 보험사, 이름, 적용 가능 여부와 상태만 표현한다. 상태의 허용값과 계약번호 형식은 미정이므로 고정 enum이나 별도 번호 필드를 만들지 않는다.
- API 오류는 `code`, `message`, 선택적 `requestId`를 사용한다. `requestId`가 있을 때는 `x-request-id` 응답 헤더와 같은 요청 추적 값이어야 한다.
- Rule/State, DB 모델, Supabase Auth 연동, 실제 로그인·동의·보험계약 API와 Midnight 관련 계약은 이번 단계 범위 밖으로 유지한다.

### 첫 수직 기능 DB 스키마 — 2026-09-17 적용 완료

- Supabase 원격 프로젝트에는 `20260917142636_initial_first_vertical` Migration이 적용됐고, 동일 SQL을 `db/migrations/`에 보존한다. `users`, `user_roles`, `consents`, `insurers`, `insurance_contracts`, `special_contracts` 여섯 테이블과 FK 인덱스·`updated_at` 트리거·RLS 활성화 및 직접 접근 권한 회수가 포함된다.
- 브라우저의 업무 테이블 직접 접근은 허용하지 않으며 RLS 정책은 추가하지 않았다. 향후 Backend의 서버 전용 pg 실행 계정과 객체 단위 접근 제어 방식이 확정될 때 별도로 구성한다.
- 이 적용은 DB 스키마 보존 단계이며 Supabase Auth 로그인, Backend pg 연결, 실제 계약 조회 API, Rule/State·주행·증명·Midnight 구현 완료를 뜻하지 않는다.
- `Consent.consentedAt`은 Shared에서 필수 ISO 문자열이나 DB의 `consented_at`은 NULL을 허용한다. 동의 시각의 필수 정책은 후속 계약 결정으로 남긴다.

### Supabase Auth Backend 인증 기반 — 2026-09-18 구현

- Backend는 `Authorization: Bearer <access_token>`을 Supabase `auth.getUser(accessToken)`으로 검증한 뒤, 검증 결과의 UUID로 `public.users` 존재와 `public.user_roles.role`을 조회한다. Access Token을 단순 decode하거나 JWT metadata·클라이언트 입력으로 역할을 판단하지 않는다.
- `GET /auth/me`은 검증된 `id`, `email`, DB 역할로 구성한 Shared `User`를 반환한다. `requireAuth`와 `requireRole` middleware는 이후 업무 API의 인증·역할 기반 권한 검사 토대다.
- Auth 계정이 있어도 서비스 사용자 행이 없으면 `USER_NOT_INITIALIZED`, 역할 행이 없거나 유효하지 않으면 `ROLE_NOT_ASSIGNED`를 반환한다. 자동 사용자 생성이나 기본 `DRIVER` 역할 부여는 하지 않는다.
- 이 단계에서 DB 스키마·RLS·GRANT·시드와 기존 migration은 변경하지 않았고, 별도 Express 로그인 API도 만들지 않았다. Frontend는 Supabase Auth로 로그인하고 얻은 Access Token만 Backend에 전달한다.

### 동의·본인 보험계약·특약 선택 Backend — 2026-09-18 구현

- DRIVER는 `GET`/`POST /consent`으로 현재 동의를 조회·기록한다. 동의 row가 없으면 `CONSENT_NOT_FOUND`를 반환하며, Shared `Consent`에 null timestamp를 만들지 않는다. 현재 MVP에는 철회·버전·이력이 없다.
- DRIVER 전용 계약 API는 SQL 조회부터 `owner_user_id = req.authUser.id`을 포함한다. 타인 계약과 존재하지 않는 계약은 모두 `INSURANCE_CONTRACT_NOT_FOUND`로 처리한다.
- 특약 자체 상태와 DRIVER의 현재 선택 상태는 `special_contract_selections`로 분리한다. 계약당 한 선택을 atomic UPSERT로 유지하고, 복합 FK 및 애플리케이션 검증으로 다른 계약 특약의 선택을 막는다. `is_eligible = true`인 특약만 선택할 수 있다.
- `20260918025120_add_special_contract_selection`은 원격에 이미 적용된 migration의 저장소 동일본이다. 브라우저의 업무 DB 직접 접근, RLS/GRANT 변경, 실제 원격 fixture 입력은 하지 않았다. Rule/LLM·운행·증명·Midnight 작업은 범위 밖으로 유지한다.

### 담당 확인 — 2026-09-17

사용자는 기능 분담의 **C(Core 계산·Midnight 검증)**를 맡기로 확정했다. C의 작업 범위와 B·A와의 연결 책임은 `docs/TEAM_WORKING_PLAN.md`의 채택한 분담을 따른다. A·B의 실명 연결은 미정이다.

### Rule 관리 DB 기반 — 2026-09-18 적용 완료

- Supabase 원격 DB에는 `insurer_memberships`, `rules`, `rule_versions`를 추가했으며, `20260918100823_add_rule_management` 및 `20260918100935_fix_rule_version_trigger_search_path` migration의 동일 SQL을 저장소에 보존한다.
- `insurer_memberships`는 INSURER 역할과 실제 보험사 소속을 분리하며, 한 사용자가 여러 보험사에 소속될 수 있으므로 `user_id` 단독 UNIQUE를 두지 않는다. 현재 소속 행과 fixture는 생성하지 않았다.
- 특약 하나에는 logical Rule 하나를 연결하고, 변경 이력과 실제 Rule 내용은 Rule Version으로 관리한다. Rule Version은 JSONB 객체 정의, `DRAFT`/`APPROVED` 상태와 승인 메타데이터를 보관한다.
- DRAFT에서 APPROVED 전이는 허용하지만 승인된 Rule Version은 수정·삭제할 수 없다. 변경이 필요하면 새 Version을 만든다. Rule ID·Version·보험사·특약 연결은 관계형 데이터로 조립한다.
- 현재 demo Rule 값은 실제 보험상품의 확정 규칙이 아닌 개발용 예시다. 세 신규 업무 테이블도 RLS를 활성화하고 `PUBLIC`, `anon`, `authenticated`의 직접 권한을 회수하며, 브라우저 직접 DB 접근은 허용하지 않는다.
- 이번 단계는 Rule 관리 DB 기반 적용 완료까지다. 실제 membership row, Rule Draft·수정·승인 API, Rule E2E, Rule Hash, Midnight 등록은 아직 구현되지 않았다.

### 모의 주행 시작 계약과 migration 선행조건 — 2026-09-18 결정

- 가입자의 운행 시작 요청은 보험계약, 선택 특약, 평가기간만 전달한다. 멱등성은 `Idempotency-Key` 헤더로 식별하며, Rule Version·직전 Confirmed State·거리·점수·이벤트·할인 결과를 클라이언트가 전달하지 않는다.
- Backend가 로그인 사용자, 계약·특약, 평가기간, Active Rule Version과 직전 Confirmed State를 조회해 모의 기록을 한 번 생성하고 재시도에 재사용하는 방향이다. 이는 구현 계획이며 운행 API·생성기·Core·증명 경로는 아직 구현하지 않았다.
- 현재 저장소에는 `TripSchema`, `CalculateTripRequest`, Rule Version, Confirmed State, State Transition, Processing Job의 확정 계약·테이블·PK가 없다. 따라서 `driving_sessions`/`processing_jobs` migration은 FK 없이 만들지 않고 선행 계약이 확정될 때까지 보류한다. 원격 Supabase에는 어떤 migration도 적용하지 않았다.

1. 예시 보험사 한 곳과 안전운전 특약 한 종을 사용한다.
2. 예시 약관 한 종의 **LLM·Document Agent 규칙 초안 생성을 우선 시도**한다. 보험사 담당자가 초안을 수정·검토하고 최종 승인한다. 변환 실패 시 지원하는 규칙 값을 수기로 입력하며 동일한 검토·승인 경로를 사용한다. LLM 방식이 구현되지 않으면 제외하고 수기 입력 경로를 유지한다. 이는 기존의 수기 입력 전용 결정을 변경한 2026-09-16 합의다.
3. 가입자는 해당 특약을 선택하고 **두 번 이상의 모의 운행**을 순차적으로 처리한다. 2026-09-18 논의에 따라 주행 문서 업로드 대신 `운행 시작` 요청으로 Backend가 모의 기록을 생성하는 방향을 정리했다. 승인된 최소 거리와 이전 확정 누적 거리에 맞춰 데모 거리를 생성하고 시간·이벤트는 합리적인 관계를 갖도록 구성한다. 상세 비율·확률은 추천안이며 생성기·API·화면은 미구현이다. [모의 주행 설계](docs/DRIVING_SIMULATION.md)를 따른다. 실제 GPS, 외부 내비게이션 및 실제 보험사 시스템 연동은 데모 범위 밖이다.
4. 승인된 규칙으로 계산된 누적 결과와 이전·신규 상태의 관계를 Midnight에서 검증 가능하게 만드는 것이 핵심 기술 목표다.
5. 가입자는 보험사에 제공할 결과를 확인하고 제출을 승인한다. 보험사 화면과 API에는 원본 위치, 구간별 속도, 정확한 운행시각, 이동경로를 제공하지 않는다.
6. 보험사는 검증 결과와 특약 판단에 필요한 최소한의 결과를 보고 할인 여부를 결정한다. `증명 유효`와 `할인 승인`은 별개 상태다.
7. 계산 결과, 승인 규칙 또는 이전 상태를 바꾼 경우 유효하지 않은 계산임을 실제 검증 경로에서 확인하는 것을 목표로 한다.
8. 제출된 운행기록과 계산 입력을 연결하기 위해 **Dataset Merkle Tree를 MVP에 적용**한다. 실제 기록 생성이나 전체 운행의 완전성을 보장하는 구조로 표현하지 않는다.
9. 원본 주행데이터는 **Midnight 검증과 트랜잭션 반영 확인 후 삭제**한다. 증명 생성 또는 트랜잭션 처리 실패 시 재처리를 위해 보관한다. 2026-09-17 재시도·실패 원본 보관 기준을 채택했으며 실제 운영 구현은 후속 작업이다.
10. 동일 평가 결과의 중복 제출은 Backend 검사와 온체인 `nullifier`로 방지한다. 2026-09-17 합의로 미적용 후 새 운행으로 점수가 바뀌고 신청 조건을 만족한 새 확정 상태는 재신청을 허용한다. 심사 중·적용 완료 건의 추가 신청은 막는다.
11. 이메일 기반 로그인과 역할 구분을 MVP에 포함한다. Supabase Auth의 이메일·비밀번호 방식을 채택했다. 객체별 권한은 Backend에서 검사하며 정확한 API 계약은 구체화 전이다.
12. 가입자는 Midnight Wallet SDK를 사용하는 자체 보관형 임베디드 월렛으로 제출과 트랜잭션을 승인한다. 가입자 개인키를 Drivacy 서버에 저장하지 않으며 보험사는 별도 월렛 설치 없이 서비스를 이용한다. 다중 기기 월렛 복구는 MVP에서 제외한다.
13. 최신 개발 지시에 따라 운행별 후보 상태는 Midnight에서 최신 이전 상태와 상태 전이를 검증하고 체인 반영이 확인된 후에만 확정한다. 미확정·실패 시 이전 확정 상태를 유지한다. DB를 먼저 확정하고 검증 실패 시 되돌리는 구조를 사용하지 않는다.
14. Frontend는 React를 사용하며 디자인 완성 후 구현한다. 현재는 프로토타입을 구현 판단 대상으로 삼지 않고, 먼저 GitHub 클론 환경에서의 LLM 실행 조건을 확인한다. Backend는 Node.js + TypeScript + Express로 선정했다. Frontend는 React + TypeScript + Vite를 사용하고 DB·인증·배포 등은 docs/TECH_STACK.md를 따른다. 선정과 구현·검증 완료는 구분한다.

## 문제 제시 기준 — 2026-09-17 확정

### 가입자별 Chain Rule registration 기반 — 2026-09-19 확정

- 가입자·보험계약·특약 조합의 evaluation scope는 특약 최초 선택 시점부터 보험계약 종료일까지 이어지는 누적 평가 범위다. 시작·종료 날짜는 KST 기준으로 만들고 Backend가 최초 생성한 evaluation period ID는 Rule version 변경에도 유지한다.
- 가입자 Scope마다 별도 Chain Contract를 사용한다. 최초 APPROVED Rule은 `deployRule`로 배포하고, 이후 APPROVED version은 동일 deployment에 `updateRule`로 등록한다. v1 registration과 누적 State·운행 흐름은 삭제하거나 초기화하지 않는다.
- 실제 C adapter가 연결되기 전 B는 Fake Adapter로 오케스트레이션만 검증한다. B는 Rule Hash, Compact/Midnight deploy·update transaction, 실제 chain confirmation을 생성하거나 완료로 주장하지 않는다.
- 동일 Scope·Rule version의 동시 등록은 외부 Adapter 호출 전에 DB transaction-scoped advisory lock으로 직렬화한다. deployment 재사용은 Scope만이 아니라 현재 Adapter runtime의 network와 adapter profile까지 일치해야 한다.
- Scope Deployment의 현재 적용 Rule은 APPROVED 상태만으로 정하지 않는다. 같은 runtime Deployment에 실제 chain-confirmed registration/update가 저장된 뒤 `current_rule_version_id`로 지정된 version만 새 운행에 사용한다. 과거 운행은 생성 당시 Rule Version을 유지한다.

### Rule Draft → 검토·저장 흐름 — 2026-09-20 구현

- INSURER는 자신의 특약에 약관 텍스트로 Rule Draft를 요청할 수 있다. Gemini 결과와 설정·호출 실패 fallback은 모두 검토용 `draft` 또는 `manual_required` 응답이며 자동 승인·DB 저장을 하지 않는다.
- 저장은 기존 완전한 `RuleDraftInputSchema` 검증과 DRAFT Version 생성 API를 그대로 사용한다. null이 있는 Draft, evidence, issues, provider, 원문은 Rule Version DB에 저장하지 않는다.

사용자 지시에 따라 문제를 절대 억지로 찾지 않는다. 개발에 치명적인 경우에만 문제로 제시하며, 실제 근거와 영향을 설명한다. 단순 개선 취향이나 가정만으로 문제를 만들지 않는다. 이 기준은 Backend·Core를 포함한 프로젝트 개발과 검토에 적용한다.

## 향후 검증 방식 — 2026-09-18 확정

사용자는 Linux 독립 환경과 독립 기준 계산으로 검증한 방식을 앞으로도 적용하도록
확정했다. 구현 환경의 검사에 더해 런타임·의존성·캐시를 분리한 별도 실행 환경에서
새로 설치하고 관련 정상·실패·경계·연속 처리 사례를 검증한다. 같은 런타임/캐시를
사용하는 임시 폴더나 다른 에이전트의 코드 검토만으로 이 검증을 완료했다고 하지 않는다.

계산 로직은 구현을 참고하지 않고 명세에서 작성한 독립 기준 계산과 결과를 대조한다.
실패하면 구현·환경·검증 코드 중 원인을 구분해 수정하고 영향받은 검사를 다시 실행한다.
환경·버전·실행 명령·테스트한 소스 해시·결과·미검증 범위를 증거로 남긴다.
WSL/컨테이너와 별도 물리 장비, mock과 실제 증명·체인·provider·브라우저·배포 검증을
구분하며 기능에 필요한 실제 실행 확인을 독립 계산 검사로 대체하지 않는다.

## 임시 산식 작성과 미정 항목 처리 — 2026-09-17

### 사용자 지시 — 확정

산식은 아직 없으므로 개발 담당자가 임의로 작성한다. 데모용 임시 산식으로 구분하고 이후 실제 승인 규칙으로 교체한다. 다른 항목은 문서와 기존 사용자 답변의 결정을 우선 적용하며, 없는 항목은 추천안과 이유를 제시한다. 추천안을 사용자 승인 없이 확정 사업 정책으로 취급하지 않는다. 이 지시는 과거의 최종 산식 확정 전 구현 대기 기준보다 우선한다.

### 작성한 데모용 임시 산식 — 실제 보험상품 규칙 아님, 구현 전

- 평가기간 시작 점수는 100점, 누적 거리는 0m다. 계산 대상은 수집이 활성화된 모의 운행이며 후보 입력은 음수가 아닌 정수로 검증한다.
- `누적 감점 = 2 × 누적 과속 횟수 + 1 × 누적 급가속 횟수 + 3 × 누적 급제동 횟수`.
- `최종 점수 = max(0, 100 - 누적 감점)`. 이전에 하한 처리한 점수에서 차감하지 않고 누적 이벤트로 재계산한다.
- 누적 거리는 운행 거리의 합이다. 거리 500,000m 이상이고 점수 80점 이상이면 데모 할인 조건을 충족한다. 충족 시 80~89점은 10%, 90~100점은 12%, 미충족 시 0%다. 조건 충족·예상 할인율은 보험사의 실제 적용 결정과 구분한다.
- 첫 운행은 300,000m·과속 2회·급가속 1회·급제동 1회로 누적 300km·92점이며 거리 조건 미충족이다. 두 번째 운행은 250,000m·과속 1회·급가속 0회·급제동 1회로 누적 550km·87점이며 예상 할인율 10%다. 두 운행 모두 계산이 유효하면 증명은 유효할 수 있다.
- 위 값은 개발용 예시로 통일한다. 과거의 초기 85점, 332.4km·524.8km 등의 수치를 동시에 적용하지 않는다. 임시 Rule도 보험사 검토·승인 및 체인 등록 경로를 거쳐야 하며 미승인 초안의 자동 적용을 허용하지 않는다.

### 채택한 기준 — 2026-09-17 확정, 구현 전

사용자가 아래 추천안과 이유를 채택했다. 이전의 미정·제안 표현보다 이 합의를 우선한다. 산식은 교체 가능한 데모 임시값이며 추천안의 실제 서비스·체인 구현 완료를 뜻하지 않는다.

| 항목 | 추천 | 이유 |
| --- | --- | --- |
| 단위·표시 | 내부 거리 m 정수·시간 초 정수·점수 정수·할인율 basis point 정수. 보험사 결과에는 정확한 거리 m와 점수를 제공하고 화면만 km 소수 첫째 자리로 반올림한다. 조건 판정에는 표시값을 사용하지 않는다. | 계산과 회로의 부동소수점·반올림 차이를 피하고 기존의 정확한 숫자 제공 선호를 반영한다. |
| 보험사 결과 | 최종 점수·누적 거리·평가기간·조건 충족·예상/적용 할인율·Rule Version/Hash·검증 결과·트랜잭션 ID를 제공한다. 운행별 설명은 가입자용으로 유지한다. | 기존에 제시한 결과 항목으로 특약 판단이 가능하며 원본과 불필요한 운행별 정보를 전달하지 않는다. |
| 공개 원장·증명 입력 | Rule Hash·Dataset/State 커밋먼트·중복 방지 값·전이/최종 검증에 필요한 최소 정보로 제한한다. 원본·개인 식별정보·정확한 점수/거리는 공개하지 않는 방향으로 설계하고, 보험사에 전달하는 정확한 결과와 검증된 상태의 연결을 확인 가능하게 만든다. 구체 회로 필드는 실제 계약 설계에서 확인한다. | 보험사 전용 결과와 누구나 읽는 공개 정보를 분리하면서 결과의 바꿔치기를 막는다. |
| 재신청·중복 식별 | 동일 확정 평가 결과는 기존 신청을 반환한다. 미적용 결정 후 새로운 운행으로 점수가 바뀌고 신청 조건을 만족하면 같은 평가기간의 새 확정 State로 재신청을 허용한다. 심사 중·적용 완료 건의 추가 신청은 허용하지 않는다. 신청자·계약/특약·평가기간·Rule/확정 State 버전에 연결된 동일 평가 식별자는 재시도에도 같은 nullifier를 사용한다. | 기존의 점수 변화 후 재신청 제안을 구체화하고 재시도와 새로운 평가를 구분한다. |
| 재시도·대기 | 일시적 실패는 최초 시도 외 최대 3회, 1분·5분·15분 간격으로 자동 재시도한다. 입력/증명 무효와 승인 취소는 자동 재시도하지 않는다. 제출 이후에는 트랜잭션 상태부터 확인하며 승인 대기·체인 결과 불명은 별도 상태로 유지한다. | 일시적 실패 복구와 무의미한 반복을 구분하고 승인 대기를 오류로 처리하거나 체인 성공 작업을 다시 제출하지 않는다. |
| 실패 원본 보관 | 성공 경로는 기존대로 체인 반영 확인 후 삭제한다. 자동 재시도 소진 뒤 체인 미반영·실패가 확인된 원본은 수동 재처리용으로 최대 7일 보관하고 만료 후 삭제한다. 만료 후 재처리 불가를 안내한다. 체인 결과 불명 작업은 먼저 상태를 확인하며 단순 만료로 성공/실패를 단정하거나 입력을 삭제하지 않는다. | 수동 복구 시간을 제공하면서 확인된 실패 원본을 무기한 보관하지 않는다. |

### 1단계 B↔C 연결 계약 — 2026-09-17

사용자는 C 작업 순서의 1단계인 연결 계약 정리를 지시했고 필요한 서브에이전트·하네스 사용을 허용했다. [docs/BC_CONTRACT.md](docs/BC_CONTRACT.md)에 내부 Rule·운행·Scope·후보/확정 State·체인 처리 결과를 정리하고 `packages/shared/src/bc-contract.ts`에 TypeScript 타입·Zod 검증 스키마·고정 직렬화를 작성했다. 외부 REST API 경로나 DB·Core 계산·Compact·실제 SDK 구현은 추가하지 않는다. 공통 서버 설정·workspace·lockfile은 변경하지 않고 임시 폴더의 독립 하네스에서 계약을 검증한다.

B는 이 계약의 외부 명세 매핑·객체 권한·작업 claim·DB 조건부 확정을 연결하고 C는 실제 계산·증명·체인 결과를 구현한다. 스키마 검증과 체인 반영은 구분하며 다른 팀원의 검토·통합 완료를 주장하지 않는다. 해시/field 인코딩·Merkle 구성의 실제 어댑터 프로파일은 2단계의 작은 실제 계약 경로에서 확인 후 고정한다.

### 3단계 Core 구현과 누적 상태 유지 — 2026-09-18 확정

사용자는 승인 Rule을 읽는 누적 Core 계산, 가입자용 설명과 독립 하네스 검증을
진행하도록 승인했다. “이전 기록이 계속 이어진다”는 뜻은 원본의 영구 보관이
아니라 **누적 State·체인 이력을 다음 운행으로 계속 연결**하는 것으로 확인했다.
제출·확정·신청 후 누적값을 초기화하지 않는다. C는 계산·증명·체인 연결,
B는 DB 확정 State/이력 보존과 원본 삭제 운영을 맡는다. 원본은 기존 합의대로
실제 체인 반영 확인 후 삭제하며 비공개 State/opening과 확인 이력은 유지한다.

Core는 root/salt/commitment 없는 계산 결과를 반환한다. 후속 실제 Merkle/회로
어댑터가 완전한 후보 State를 조립하며 계산만으로 확정 상태를 생성하지 않는다.
세부 동작과 검증 범위는 [CORE_CALCULATION.md](docs/CORE_CALCULATION.md)를 따른다.

2026-09-18 새 독립 하네스에서 strict TypeScript 검사와 Core 27개·공유 계약
26개, 총 53개 테스트가 통과했다. 첫 계산 결과를 fixture 확정 State로 연결해
다음 운행의 누적값과 이전 커밋먼트 참조를 검증했다. 실제 보험 회로/Merkle
연결이나 B의 DB 이력 저장·원본 삭제 동작은 이 계산 검증에 포함되지 않는다.

사용자의 후속 지시에 따라 버그 디버깅은 별도의 실행 환경에서도 진행한다.
새 Windows 임시 폴더의 검사와 독립 Codex 코드 검토를 다른 OS 환경 검증으로
대체해 표현하지 않는다. Linux 임시 파일시스템·별도 Node 바이너리·빈 npm
캐시·새 의존성 설치로 Core를 실행하고, 구현을 보지 않고 명세에서 작성한
Python 정수 기준 계산과 누적 결과를 대조한다. 같은 물리 PC의 WSL 환경이며
별도 서버/물리 장비 검증으로 주장하지 않는다.

2026-09-18 Linux 독립 환경에서 타입 검사와 기존 53개 테스트, Python 기준
계산 대조 332개·잘못된 입력 거부 15개가 통과했다. 새 검증 러너의 Node 타입
설정 및 입력 uint32 초과 오류코드 기대값을 보완했고, Core 구현 변경 없이
같은 소스 SHA256으로 검증했다. 이 범위에서 Core 계산 버그는 확인되지 않았다.

### 4단계 상태 전이 증명과 학습 방식 — 2026-09-18 확정

사용자는 4단계를 구현하면서 원리·코드·검증 근거를 함께 설명하도록 요청했다.
Rule·Dataset·State를 먼저 구분하고 예시 계산 → 실제 회로 제약 → 변조 거부
검증 순서로 진행한다. 실제 실행 규격과 학습 설명은
[STATE_TRANSITION_PROOF.md](docs/STATE_TRANSITION_PROOF.md)에 기록한다.

후속 학습 요청에 따라 회로·State/Merkle 어댑터·로컬 검증 하네스에 한국어
주석을 작성했다. 각 제약의 목적과 공개/비공개 경계·B/C 책임을 설명하며
계산식·조건·실행 순서는 변경하지 않는다.

기존 B↔C의 1~1,024개 records를 처리하기 위해 운행 시작 → 순번별 Merkle
포함 검증·비공개 누적 → 모든 기록 처리 후 운행 확정으로 나눈다. 중간에는
이전 확정 State를 유지한다. 이는 C의 실행 구조이며 새로운 사업 정책이나
B의 운영 구현 완료가 아니다. Rule 등록 Hash·Scope binding을 deployment에
고정하고 변경 가능한 계산 계수는 회로의 RuleOpening 해시에 포함한다.
보험사 승인·객체 권한·DB 확정은 기존대로 B, 가입자 월렛 승인 연결은 후속
C의 SDK 작업이다. 컴파일/회로 테스트와 실제 로컬 proof·체인 검증은 구분한다.

2026-09-18 새 독립 하네스에서 전체 ZK 컴파일·strict 타입 검사·72개 테스트
(회로 19, Core 27, 공유 계약 26)가 통과했다. 실제 로컬 proof/계약 제출 9회로
초기화·두 운행 확정(revision 2)과 계약 재연결을 확인했다. 점수·Rule·이전
opening·owner secret·기록 조작·누락·오래된 상태 9건은 추가 proof 요청/제출
없이 거부됐다. [실행 증거와 소스/ZK 키 해시](docs/evidence/driving-state-2026-09-18.json)를
보관한다. 실제 운행 입력은 모의 기록 1개 + 2개이며 최대 기록 수의 전체 체인
실행 성능을 측정한 것이 아니다. B의 DB/삭제 운영·실제 승인 권한, Preprod와
가입자 브라우저 승인 E2E·최종 신청/nullifier·보험사 결과 공개 연결은 별도다.

### C 5단계 착수와 학습 방식 — 2026-09-18

사용자는 구현과 함께 작업 방식·근거를 공부하고 한국어 주석으로 설명을 남기는
방식을 요청했으며, 이 방식의 메모리 저장과 기존 순서의 다음 단계 진행을 지시했다.
다음 단계는 월렛 승인·B 작업 연결이며 최종 신청/nullifier보다 먼저 진행한다.
작은 예시 → 코드의 검사 목적 → 정상/변조 검증 근거를 함께 설명한다.

[월렛 승인과 작업 제어 문서](docs/WALLET_APPROVAL_JOBS.md)에 C의 승인 인터페이스,
단계 순서, 제출 전 거래 ID 기록, 결과 불명 조회, 확정 결과 재조회와 기존 합의의
1·5·15분/추가 3회 재시도 제한을 정리했다. 실제 SDK 호출 앞뒤에 훅을 연결하고
이미 성공한 단계는 다시 제출하지 않는다. B는 신뢰된 C 결과를 받은 후 별도로
작업 claim·DB 이전 State CAS·원본 삭제를 처리한다. B API/DB·A 화면은 수정하지 않는다.
임시 하네스의 JSON journal과 로컬 개발자 승인은 운영 분산 저장소·가입자 브라우저
승인으로 취급하지 않으며, 전체 가입자 월렛 및 운영용 BCAdapter 통합은 후속 작업이다.

2026-09-18 전체 컴파일·strict 타입 검사·88개 테스트와 실제 로컬 두 운행
(revision 2)이 통과했다. 개발자 승인 7회·proof/계약 제출 각 9회였으며, 실제
체인 성공 뒤 결과 반환 장애를 주입한 복구와 확정 결과 재조회에서 추가 제출이
없었다. [5단계 실행 증거](docs/evidence/wallet-jobs-2026-09-18.json)를 별도 보관한다.
사용자 요청으로 작업 방식도 점검했으며, C 구성 요소 검증과 가입자/B 전체 통합
완료를 구분하고 합성 receipt 검사만으로 실제 복구를 주장하지 않는 기준을 유지한다.

### C 6단계·기기 월렛·B 연결 — 2026-09-18 후속 확정

사용자는 기존 다음 단계까지 진행한 뒤 Claude Midnight Expert로 지금까지의
구현을 교차검증하도록 요청했고, 같은 세션에서 ‘미드나잇’ 도구 활성화를 명시했다.
가입자 기기에서 월렛 키를 사용하고, C의 실제 확정 결과를 B의 이전 State CAS와
작업 claim에 연결한 뒤 원본을 삭제하는 구성 요소를 진행한다. 이는 위 5단계에서
보류했던 B 연결의 후속 승인이다. 제품 화면·임의의 공개 REST 경로를 만들지 않는다.

최종 결과 회로는 최신 확정 State에서 Rule·누적 결과를 다시 검증하고 비공개
보험사 결과 commitment와 같은 State 중복 제출 방지 nullifier를 만든다.
결과 salt를 바꾸어 중복 검사를 피하지 못해야 하며 누적값을 초기화하지 않는다.
독립 검토에서 재현한 witness 바인딩 문제를 수정한 v3 adapter profile과 새 로컬 deployment를 사용하고
이전 v1/v2 증거는 유지한다. 인증과 nullifier는 한 번 읽은 동일한 비밀값에 제약한다.
제출 전 비재시도 실패만 전체 journal 확인 후 작업을 포기하고 미확정 원본은 보관한다.
보험사 승인·적용 결정과 pending/applied 재신청 정책을
이 회로가 대신 완료했다고 표현하지 않는다.

브라우저 월렛과 PostgreSQL 연결 검사는 별도 Chromium 프로파일·공개 local
genesis·모의 계약·원본 파일을 사용한다. 원격 Supabase migration 적용, 실제
가입자 provisioning/Auth/Storage·제품 화면·Preprod는 별도 검증 범위다.
구현·실행 근거와 한계는 [FINAL_EVALUATION.md](docs/FINAL_EVALUATION.md)에 남긴다.

2026-09-20 이전 단계가 제출된 뒤 terminal failure가 발생한 운행의 체인 취소·C/B 작업 종료를 연결했다. C는 전체 journal에서 이전 거래의 확정/거부 상태를 확인하고, 가입자가 별도 `cancelTrip` 거래를 승인해 실제 receipt가 확인된 뒤에만 B의 scope 해제를 허용한다. 결과 불명 거래가 있거나 `finishTrip`이 이미 확정된 작업은 계속 막고, 원장 확인 없이 DB 작업만 해제하지 않는다. 기존 작업당 재시도 세 번 정책은 유지한다.

같은 날 Docker Desktop의 stale 로컬 socket을 백업명으로 보존 이동한 뒤 v3 전체 로컬 연결을 재검증했다. 6개 회로 전체 ZK 컴파일·strict 검사·106개 테스트와 실제 로컬 브라우저 월렛/격리 PostgreSQL 연결이 통과했다. 제출 후 취소, DB scope 해제, 두 운행 revision 2, 최종 평가/nullifier, 변조·중복 10건 거부를 확인했다. 이 결과는 공개 local genesis와 fixture DB 근거이며 실제 가입자 Auth/Storage·제품 화면·원격 Supabase migration·Preprod 검증은 아니다.

## 개발 착수 기준과 최초 점검 — 2026-09-16

대상은 **2026 미드나잇 해커톤**이다. 문서 우선순위는 최신 사용자 명시적 지시 → 최종 API 명세서·데이터 플로우 → 최종 기획 문서 → 기존 코드·주석 → 일반 개발 관행이다. 문서에서 제안·미정으로 표시한 내용을 임의로 확정하지 않는다. 구현을 막지 않는 임시값은 확정 사업 규칙과 분리하여 표시한다.

최신 지시는 이메일 로그인, 자체 보관형 임베디드 월렛과 체인 검증 후 상태 확정을 MVP 기준으로 지정했다. 따라서 기존 문서의 월렛 미확정 표현과 아래의 과거 신청 시 일괄 증명 제안보다 이 기준이 우선한다. 별도 변경 지시 전에는 운행별 검증·확정과 최종 확정 상태를 이용한 신청 흐름을 따른다.

확인한 자료는 다음 세 개다.

- [사업계획서](https://docs.google.com/document/d/1y26KcwcMx-VlILhsubqiB-TQxbYpC9wq4IacrpMOe0g/edit): 제품 목적, 참여 주체, MVP와 고도화 범위.
- [Backend 통합 흐름 기술 설계](https://docs.google.com/document/d/1zbBt5njzajgOpBJQZQ0E6d2jH2V8EnLA8waYlNoOHqs/edit): 운행별 상태 검증·확정, 최종 신청, 데이터 보관·공개 및 검증 책임.
- [개발 범위·화면·API 시트](https://docs.google.com/spreadsheets/d/1F24KnjbSbAjASHNdG83hZBqFFhPUJKLE-PwJTJ703RU/edit): 기능 ID, 화면별 행동과 Backend 처리, API 경로 제안. 시트 52행은 URI·상태코드를 구현 제안으로 표시하고 55행은 인증 API, 87행은 구체 응답 스키마·오류코드가 미정임을 명시한다. 현재 제공본을 완결된 외부 API 계약으로 취급하지 않는다.

최초 점검 당시 로컬 `feat` 체크아웃에는 `AGENTS.md`, `CLAUDE.md`, `PROJECT_DIRECTION.md`, `README.md`만 있었다. 후속 문서화에서 `docs/`를 추가했으며 앱·HTML 소스, 패키지 설정, 환경변수 예시, DB 스키마, 테스트와 배포 설정은 아직 없다. 시트 110행의 저장소 내 HTML 프로토타입 설명은 최초 점검에서 확인되지 않았으며, 공유된 와이어프레임 사이트와 구분한다.

후속 위임으로 Frontend·Backend·DB·인증 기술 스택은 선정 완료다. 첫 수직 기능의 정확한 API 계약은 구현 전 구체화한다. 첫 기능은 이메일 로그인·동의 후 본인 모의 보험계약을 조회해 화면에 표시하는 흐름을 제안한다. 시트의 `GET /api/v1/contracts?query={query}`를 기준 후보로 사용하되 요청·응답 스키마와 오류 규약을 임의 생성하지 않는다. Rule/State 스키마, 회로 입력·공개 범위, 네트워크·SDK 조합, 운행별 트랜잭션 승인 상세와 재신청 정책은 해당 기능 구현 전에 확정한다.

후속 사용자 확인으로 Frontend React는 정해졌으며 화면 구현은 디자인 완성 후 착수한다. 위 첫 화면 포함 수직 기능은 이후 구현 제안으로 남긴다. Backend·DB·인증은 TECH_STACK의 조합으로 선정했다. 외부 API 계약은 구체화 전이다. 임베디드 월렛의 Midnight Wallet SDK 사용은 사업계획서에도 명시돼 있으므로 미확정인 것은 사용 여부가 아니라 구체적인 SDK 버전·승인 구현이다. 네트워크는 로컬 확인 후 Preprod를 사용한다.

### 클론 환경에서의 LLM 확인 — 2026-09-16

공개 저장소를 별도 임시 디렉터리에 새로 클론했다. 기본 브랜치는 `main`, 확인 커밋은 `4445e9a67eaee6ba0e1c3e53cc52e82a83f11234`이며 문서 4개만 있다. LLM 호출 코드, 패키지 설정과 환경변수 예시가 없으므로 현재 제출 코드에서 LLM이 실행된다고 주장하지 않는다.

Gemini 공식 모델 목록 REST API를 API 키 없이 호출한 결과 HTTP `403 PERMISSION_DENIED`로 인증을 요구했다. 이 확인은 모델 목록 조회이며 생성 요청·규칙 추출 성공·비용·성능을 검증한 것은 아니다. REST 사용은 SDK 의존성을 줄일 수 있지만 인증·모델 접근·사용량 제한을 없애지 않는다.

후속 합의로 **클론한 앱 → 팀 배포 Backend → LLM API**를 기본 실행 방식으로 확정했다. 심사위원은 LLM 키를 입력하지 않는다. 레포에는 서버의 공개 주소를 기본 설정으로 넣고 실제 키는 팀 서버의 환경변수 또는 Secret에 보관한다. 모델·프롬프트·규칙 출력 형식은 팀 Backend에서 관리한다. 공급자는 Gemini 안정판 Flash, 기본 모델은 TECH_STACK 기준이며 호스팅은 Cloud Run으로 선정했다. 실제 서버 주소는 배포 전이고, 서버 연결 코드와 실제 생성 호출은 아직 구현·검증하지 않았다. 인터넷과 운영 중인 팀 서버가 필요하다. 로컬 자체 키 방식은 선택 실행안이며 LLM 실패 시 수기 입력 경로를 유지한다. 자세한 결정과 제출 전 확인 기준은 [docs/LLM_EXECUTION.md](docs/LLM_EXECUTION.md)에 정리한다.

연결된 **Locate Google Sheets spreadsheet** 채팅의 사용자 지시·팀 답변과 확정·제안·미정 구분은 [docs/README.md](docs/README.md)에 정리했다. 무료 범위 활용은 검토 사항이며 공급자·호스팅·사용량을 선정하기 전에 총비용 0원으로 확정하지 않는다.

2026-09-16 사용자가 전달한 본사 리뷰·심사 안내에 따르면, 앞서 우려한 버전 불일치는 LLM 모델 버전이 아니라 컴파일 관련 문제였으며 이번 해커톤 심사에서는 Claude Code 리뷰 스킬을 사용해 컴파일 버전을 바꿔가며 확인할 예정이다. 이 안내를 작업 기준에 반영해 컴파일 버전 차이 자체를 심사 차단 우려나 추가 선행 결정사항으로 다루지 않는다. 심사위원의 LLM 키 입력을 없애는 팀 서버 방식은 유지한다. 이는 사용자가 전달한 안내 기록이다.

## 백엔드 명세 질의 답변 반영 — 2026-09-16

사용자가 전달한 2026-09-16 당시 팀 답변을 아래처럼 구분한다. 검토 요청이나 제안을 확정 사양으로 바꾸지 않는다. 이 표 이후 2026-09-17 채택한 단위·공개 범위·재신청·재시도·보관·임시 산식은 위의 최신 기준을 우선한다.

| 항목 | 합의 수준 | 명세 작성 기준 |
| --- | --- | --- |
| 1. 평가 상태 | 처리 완료의 의미 확정 | `처리 완료`는 보험사의 할인 적용·미적용 결정까지 끝난 상태다. 제안된 접수 → 증명 생성/검증 중 → 검증 완료 → 보험사 판단 대기 → 적용/미적용 흐름을 바탕으로 하되, 세부 상태명과 실패·재처리 상태는 구현 설계에서 정한다. |
| 2. 보험사 제공 결과 | 구체적인 점수·거리 제공 방향, 최종 확정 전 | 최종 점수와 누적 거리를 정확한 숫자로 제공하는 쪽을 선호하지만 답변에 추가 고민이 명시됐다. 평가기간, 조건 충족 여부, 예상/적용 할인율, Rule Version, 검증 결과, 트랜잭션 ID를 포함한 최종 항목·정밀도와 공개 ledger/Public Input 범위는 별도로 확정한다. |
| 3. 점수 변화·계산 근거 | 제시한 항목과 원본 비공개 원칙 확인 | 이전·변경 점수, 증감, 운행 거리·소요시간, 과속·급가속·급제동 집계 횟수, 항목별 가점·감점, Rule Version을 설명 항목으로 다룬다. 가입자용 설명과 보험사에 제공할 계산 근거의 세부 범위는 구분한다. GPS 위치·이동경로·구간별 속도·정확한 운행시각은 제공하지 않는다. |
| 4. Rule 등록 | 확정 | 약관 업로드 → LLM 초안 → 보험사 수정·검토 → 최종 승인 → Rule Hash 생성 → Midnight 등록. LLM 실패 시 수기 입력 후 같은 승인 경로를 사용한다. 내부 승인과 체인 등록 완료는 구분한다. |
| 5. 증명 시점 | 과거 신청 시 일괄 증명 선호·검토 요청 | 당시 일괄 증명안을 검토했으나 이후 최신 개발 지시는 운행별 Midnight 검증·체인 반영 후 상태 확정을 요구한다. 현재 구현 기준은 최신 지시와 데이터 플로우다. |
| 6. Merkle Tree | Dataset 적용 확정, Driver 필수 채택 아님 | Dataset Merkle Tree를 넣는다. 사용자 신원 자체를 숨기는 서비스는 아니라는 답변에 따라 Driver Merkle Tree를 필수 사양으로 확정하지 않는다. 신청자·계약·특약의 자격 연결 방식은 별도 설계한다. |
| 7. 삭제·실패 | 확정, 세부 운영 미정 | 검증 및 트랜잭션 반영 확인 후 원본 삭제. 실패 시 원본을 보관하고 재처리한다. 신청 전 보관기간, 재시도 한도와 최종 실패·미신청 시 정리 기준은 미정이다. |
| 8. 중복·재신청 | 중복 방지 확정, 재신청 제안 | Backend와 `nullifier`로 동일 평가 결과의 중복 등록을 막는다. 미적용 후 점수 변화에 따른 재신청 허용 여부, 기존 신청으로 이동/차단하는 UX와 새 평가 결과의 식별 범위는 미정이다. |
| 9. 데모 수치 | 임시값 사용, 최종 규칙 미정 | 초기 85점·운행 후 87점, 332.4km·524.8km, 최소 500km, 10%·최대 12%는 임시 예시다. 레퍼런스 검토 후 산식·신청 거리·점수별 할인 구간을 정하고 화면을 통일한다. |

### 과거 신청 시 일괄 증명안의 검토 기준 — 현재 구현 기준 아님

한 번의 최종 증명으로 여러 운행의 계산과 누적 관계를 검증하는 설계를 검토할 수 있다. 이를 위해 Dataset Root에 묶인 기록이 실제 계산 입력과 연결되고, 승인 Rule 및 시작 상태에서 최종 누적 상태에 이르는 계산 관계가 회로로 제약되어야 한다. Root만 등록하거나 Backend의 최종 점수만 입력하는 것으로 기존의 누적 상태 검증 목표를 충족했다고 보지 않는다. 구체적인 회로 구성과 처리 가능한 운행 수는 아직 구현·성능 검증 전이다.

이 안을 채택하면 운행 종료 시 DB에 저장한 점수와 상태는 최종 증명 전의 계산 결과다. 매 운행마다 Midnight에서 검증·확정됐다고 표시하지 않는다. 운행별 체인 상태 갱신을 보여주는 데모 목표를 유지할지, 최종 증명 안에서 두 운행의 누적 계산을 검증할지는 증명 시점 결정과 함께 정한다.

원본을 최종 검증·트랜잭션 반영 후 삭제한다는 합의와 결합하면, 일괄 증명에 필요한 기록은 운행 종료 후 신청·검증 완료까지 보관할 필요가 있다. 보관 기간이 운행 처리 시간만으로 끝난다고 안내하지 않으며, 신청하지 않은 기록과 최종 실패 기록의 정리 기준은 별도 확정한다.

### 검증의 한계

#### 운행 선택과 제출 Dataset 검증의 구분 — 2026-09-18 확정

가입자의 실제 운행 전체를 제출하도록 요구하지 않는다. 가입자가 반영할 운행을 선택하는 서비스 전제로, 미반영 운행은 부정적인 이벤트뿐 아니라 거리 누적·긍정적인 평가 기여도 반영되지 않는다. 실제 운행 전체의 제출을 강제하거나 미제출을 부정행위로 간주하는 기능은 이번 합의로 추가하지 않는다. 선택 시점과 화면 동작의 세부 사항은 이 합의만으로 새로 확정하지 않는다.

선택해 제출한 운행의 Dataset은 별개다. 해당 Dataset에 커밋한 기록을 누락·중복·변조 없이 처리하고 승인 Rule 및 최신 이전 State와 신규 State의 관계를 검증한다. 이 Dataset 처리의 완전성과 실제 운행 전체의 제출 여부를 혼동하지 않는다. 기존 서비스의 채택 여부는 이번 문서화에서 외부 검증하지 않았다.

영지식증명은 **제출된 기록에 대해 승인 규칙과 상태 전이 관계가 성립하는지**를 다룬다. 기록이 실제 운행에서 나왔는지, 빠진 운행이 없는지, 모든 운행이 제출됐는지, 원본이 실제 삭제됐는지, 보험사가 받은 결과를 재사용하지 않는지까지 증명하지 않는다. Compact의 비공개 입력이나 witness가 진짜 운행 데이터라는 보장도 없다.

현재 설계에서 원본은 Drivacy의 계산·증명 처리 영역에 일시적으로 존재할 수 있다. 비공개 대상은 보험사와 공개 원장이며, 플랫폼 자체에도 원본이 보이지 않는다고 주장하지 않는다.

## 발표용 데모 흐름 — 제안, 세부값 미확정

아래는 팀의 발표와 구현 논의를 위한 권장 시나리오다. 수치와 화면 구성은 아직 확정 사양이 아니다.

1. **보험사 화면:** 예시 약관을 업로드해 LLM 초안을 만들고 담당자가 수정·검토해 버전 `v1`으로 승인한다. 변환 실패 시 지원되는 규칙값을 수기로 입력한다. 승인본의 Rule Hash 생성과 Midnight 등록 상태를 구분한다.
2. **가입자 화면:** 특약을 고른 뒤 첫 번째 모의 운행을 처리한다. 점수는 통과하지만 최소 누적 거리가 부족한 상태를 보여준다.
3. **가입자 화면:** 두 번째 운행을 처리해 기존 상태에 연결한다. 누적 거리와 점수가 함께 기준을 만족한다.
4. **제출 확인:** 가입자가 보험사·특약·평가기간과 공개될 결과를 확인하고 신청한다.
5. **보험사 화면:** 원본 없이 규칙 버전, 조건 충족 여부와 실제 Midnight 검증 결과를 확인한 뒤 할인 승인한다. 가입자에게 결정 결과를 보여준다.
6. **발표용 검증:** 정상 요청은 성공하고, 점수·규칙·이전 상태를 조작한 요청은 증명 생성 또는 검증에서 거부됨을 보여준다. 정상적으로 계산된 `할인 조건 미충족`은 **유효한 증명**일 수 있으며, `증명 무효`와 구별한다.

이전 논의에 사용한 **가상 예시**는 최소 100km, 80점 이상, 10% 할인, 과속 1회당 2점 감점, 급제동 1회당 3점 감점이다. 첫 운행 60km·과속 2회·급제동 1회라면 93점, 두 번째 50km·과속 1회·급제동 1회까지 누적하면 110km·88점이 된다. 이는 현재 화면의 임시 500km 시나리오와 별개인 과거 예시이며, 실제 보험상품이나 최종 산식으로 인용하지 않는다. 최종 데모 수치는 레퍼런스 검토 후 통일한다.

## 구현 우선순위와 도구

### 기능 분담과 작업 조건 — 2026-09-16

사용자는 새 브랜치 생성을 금지하고, 프론트 구현을 우선 제외한 기능 분담과 공통 작업 원칙 문서화를 요청했다. 개발자는 프론트+백엔드 1명과 백엔드 위주 2명으로 총 3명이라고 정정했다. Core·Agent·Midnight를 포함한 기능을 세 개발자에게 배정한다. 이번 작업은 기존 `feat`에서 문서만 작성하며 브랜치 생성·전환을 하지 않는다.

[docs/TEAM_WORKING_PLAN.md](docs/TEAM_WORKING_PLAN.md)에 A는 약관·LLM 규칙 초안, B는 인증·계약·규칙 승인·운행 API·DB 확정·원본 운영·신청·보험사 판단·서버 배포, C는 Rule Hash·Core 계산·Merkle·Compact·Midnight 검증·nullifier를 맡는 기능 분담을 작성했고 사용자의 추천안 채택 지시로 개발 기준으로 확정했다. A의 향후 프론트 작업을 고려해 Backend 범위를 좁혔다. 월렛 SDK와 승인 인터페이스는 C, 향후 프론트 연결은 A가 담당하는 안이다. B는 C의 실제 체인 반영 확인 결과를 받은 뒤 DB 상태를 확정한다. 기술 스택과 공통 작업 원칙도 사용자의 위임으로 채택했다. 실명 배정과 API/Rule/State 계약·보험 규칙·사업 정책의 세부 사항은 구체화 전이다. 선정 기준은 [docs/TECH_STACK.md](docs/TECH_STACK.md)에 기록한다.

### 유사 사례 참고 목적과 개발 도구 역할 — 2026-09-16 합의

- `midnight-awesome-dapps` 비교의 목적은 아이디어의 독창성·우열 평가가 아니라, Drivacy와 구조가 비슷한 구현에서 **Dataset 커밋먼트, Merkle Tree, 입력 연결과 상태 전이 검증 패턴**을 찾아 참고하는 것이다. 기존 소스의 적용 가능성을 확인하되, 구체적인 leaf·해시·직렬화·솔트·경로·회로 사양과 코드 채택은 아직 확정하지 않았다.
- 사용자는 Codex에 **MIDSKILLS 플러그인을 설치할 예정**이며, 설치 후 해당 개발 지원 기능을 활용하는 방향이다. 이번 합의는 설치 완료·호환성 검증을 의미하지 않는다.
- **Claude Code를 호출할 때는 사용자가 ‘미드나잇 엘리펀트’라고 부르는 개발 지원 도구를 사용한다.** 기존 조사에서 확인한 이름은 Midnight Expert이며, 두 이름의 동일성은 아직 확인되지 않았다. 실제 호출 전에 대상 도구를 확인하며, 이번 합의만으로 Claude Code나 해당 도구를 호출한 것으로 기록하지 않는다. Midnight 도구의 현재 세션 활성화 제한도 유지한다.

팀은 **Claude Code의 Midnight Expert를 활용해 초반 개발 틀을 만들 계획**이다. 그 도구를 사용했다는 사실만으로 Midnight 기능이 구현·검증된 것으로 보지 않는다. 생성된 Compact 코드, 클라이언트, 네트워크 연동을 실제로 컴파일하고 정상·실패 케이스를 확인해야 한다.

2026-09-16 후속 지시에서도 Claude와 Midnight 개발 지원 도구를 함께 사용할 계획을 확인했다. 사용자가 ‘엘리펀트’라고 언급한 도구와의 동일성은 미확인이며 GitHub에서 확인한 이름은 Midnight Expert다. 오픈소스 후보는 `create-mn-app`, `example-bboard`, 공식 `midnight-wallet`, `midnight-local-dev`, OpenZeppelin `compact-contracts`이며, [docs/MIDNIGHT_OPEN_SOURCE.md](docs/MIDNIGHT_OPEN_SOURCE.md)에 용도·라이선스·조사 커밋을 기록했다. 후보 조사는 설치·도입 확정이나 호환성 검증이 아니다. Midnight Expert README에 따라 Windows에서는 WSL2 개발 경로를 추천한다.

추가 요청으로 ZK의 Compact 표준 라이브러리·Merkle 테스트·nullifier 예제·ZKLoan 조건 평가와 `midnight-zk`를 조사했다. 약관 처리·규칙 입력·검증·테스트에는 PDF.js, Zod, React Hook Form, Vitest를 추천 후보로 함께 기록했다. 기존 기능 구현에 필요한 소스·라이브러리 재사용을 검토하는 방향이며, 패키지 설치·버전 채택·PDF 파일 규격·인증/DB 서비스 선정은 이번 조사에서 확정하지 않았다.

규칙 등록은 LLM 초안 생성을 우선 시도하고 수기 입력 경로를 유지한다. 전체 구현은 기술 스택·첫 API 계약 확정 → 이메일 로그인·동의·본인 모의 계약 조회 → Rule 초안 생성·검토·승인 → 임베디드 월렛과 실제 증명 경로 연결 → Dataset Merkle Tree 및 두 운행의 체인 검증 후 누적 상태 확정 → 최종 신청·보험사 결정 → 재현 가능한 클론·실행 안내를 권장한다. 가입자 임베디드 월렛은 MVP에 포함하며, SDK의 구체 버전은 실제 연결 시 고정하고 네트워크는 로컬 검증 후 Preprod를 사용한다. 실제 보험사 REST 연동은 MVP 범위 밖이다.

### C 2단계 실행 방향 — 2026-09-17 확정

사용자는 다음 단계 진행과 Midnight 레포·개발 스킬의 적극 활용을 요청했다.
C는 작은 실제 Compact 계약의 전체 컴파일 → 로컬 node/indexer/proof-server
상태 확인 → 배포·증명·두 차례 상태 전이·오래된 상태 거부를 먼저 확인한다.
환경 연결을 먼저 검증해 후속 보험 산식·Merkle 회로의 오류와 구분하기 위한
순서다. 공식 create-mn-app 템플릿의 버전을 고정하고 개발 스킬을 직접 참고한다.
이 기술 검증은 승인 Rule·보험 계산·Dataset 연결·가입자 권한·nullifier의
구현 완료를 의미하지 않는다. 실제 실행 안내와 검증 범위는
[MIDNIGHT_LOCAL_PROBE.md](docs/MIDNIGHT_LOCAL_PROBE.md)에 기록한다.

2026-09-17 작은 상태 전이 계약의 전체 컴파일·SDK 타입 검사와 실제 로컬
배포·proof 생성·두 차례 체인 갱신(revision 2)·오래된 상태 거부·계약 재연결을
확인했다. onchain-runtime 중복 설치로 발생한 StateValue 실행 오류는 protocol의
3.0.0 pin과 npm dedupe로 해결했다. Preprod와 가입자 브라우저 승인 E2E는
이 검증에 포함되지 않는다.

## 미해결 질문

2026-09-17 내부 Rule/State 필드와 데모 임시 산식·공개 범위·재신청·재시도·실패 보관 기준은 정리했다. 아래는 후속 실제 구현에서 확인할 기술 사항이며 이미 채택한 사업 기준을 다시 선택하도록 요구하는 항목이 아니다.

- 원본을 비공개 입력으로 사용하는 Compact 회로에서 계산 및 상태 전이를 어디까지 제약할 것인가?
- 운행별 상태 전이와 최종 신청을 어떤 회로 구조로 구성하고 Dataset Root를 어떻게 연결할 것인가?
- 비공개 최종 결과와 검증된 State 연결을 어떤 실제 회로·어댑터 증거로 확인할 것인가?
- Dataset Merkle의 실제 해시·padding·경로 인코딩과 신청자 자격 연결을 실제 계약에서 어떻게 구현할 것인가?
- 채택한 원본 삭제·실패 재처리 기준의 실제 Storage·증명 처리 영역 동작을 어떻게 확인할 것인가?
- 지갑 및 로컬/테스트넷의 실제 재현 방법은 무엇인가?
- 실제 보험사가 외부 계산 결과를 독립 검증하기 위해 비용을 낼 필요가 있는지 확인할 수 있는가?

## 대화에서 방향이 바뀌면

결정된 내용을 이 문서의 해당 섹션에 반영하고 날짜를 갱신한다. 확정·제안·미결정을 구별하며, 기존 결정과 충돌하면 최신 결정과 그 이유를 기록한다. 실행 방법, 구현 상태, 제출 설명이 달라졌다면 README도 함께 갱신한다. 개인 대화나 민감정보는 그대로 복사하지 않고 프로젝트에 필요한 결정만 남긴다.
