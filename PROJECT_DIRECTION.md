# Drivacy 프로젝트 방향성

마지막 업데이트: 2026-09-17 (KST)

이 문서는 팀과 개발 도구가 공유하는 현재 방향의 기준이다. 실제 구현·검증 결과는 README와 코드가 증명하며, 이 문서의 계획을 구현 완료로 표현하지 않는다.

## 프로젝트와 해커톤

Drivacy는 가입자의 상세 주행기록을 보험사에 제공하지 않고, 보험사가 승인한 안전운전 할인 조건의 충족 여부를 Midnight의 영지식증명으로 검증하는 데모를 목표로 한다. 대상은 Midnight Korea Hackathon 2026이며, 공식 프로그램 페이지의 제출 마감은 **2026-09-28 00:00 KST**다. 팀은 Luma 참가 등록을 완료했다.

- 공개 저장소: https://github.com/0xyuanyx/Midnight-Drivacy
- 해커톤 안내: https://www.hackathon.midnightkorea.org/kor
- 심사에서 확인하는 것: 저장소 클론과 컴파일, 제출 폼과 README의 일치, Midnight 기능의 명확성, 접근 가능한 데모.
- 제출에 필요한 내용: 프로젝트명, 한 줄 설명, README가 있는 공개 저장소, 실행 또는 데모 흐름, Midnight 구현 포인트. 데모 영상은 선택 항목이다.
- 팀 구성의 최신 확인: 개발자는 총 3명으로 프론트+백엔드 1명(백엔드 비중 적게), 백엔드 위주 2명이다. 기능 분담은 A·B·C로 표기하며 실명 연결은 미정이다. 이전의 기획 겸 개발·프론트엔드 겸 백엔드·백엔드 구분보다 이번 개발 인원 확인을 우선한다. 디자인 역할은 기존 기록에 1명으로 남아 있으며 [기능 분담안·공통 작업 원칙](docs/TEAM_WORKING_PLAN.md)을 참고한다.

## 확정된 MVP 방향

### 첫 수직 기능 Shared 계약 — 2026-09-17 확정

- `packages/shared`는 첫 수직 기능에서 공통으로 쓰는 `Role`, `User`, `Consent`, `InsuranceContract`, `SpecialContract`, `ApiError`, `RequestId`의 Zod 스키마와 TypeScript 타입을 public export로 제공한다.
- 역할은 `DRIVER`, `INSURER` 두 종류다. Shared `User`는 애플리케이션 경계에 필요한 ID·이메일·역할만 표현하며 Supabase Auth 전체 객체를 노출하지 않는다.
- Consent는 동의 여부와 동의 시각만 표현한다. 동의 종류와 버전은 근거 자료에서 확정되지 않아 이번 계약에 포함하지 않는다.
- 보험계약은 소유 사용자, 보험사, 보험기간, 상태와 연결 가능한 특약 목록을 표현한다. 특약은 계약 연결, 보험사, 이름, 적용 가능 여부와 상태만 표현한다. 상태의 허용값과 계약번호 형식은 미정이므로 고정 enum이나 별도 번호 필드를 만들지 않는다.
- API 오류는 `code`, `message`, 선택적 `requestId`를 사용한다. `requestId`가 있을 때는 `x-request-id` 응답 헤더와 같은 요청 추적 값이어야 한다.
- Rule/State, DB 모델, Supabase Auth 연동, 실제 로그인·동의·보험계약 API와 Midnight 관련 계약은 이번 단계 범위 밖으로 유지한다.

1. 예시 보험사 한 곳과 안전운전 특약 한 종을 사용한다.
2. 예시 약관 한 종의 **LLM·Document Agent 규칙 초안 생성을 우선 시도**한다. 보험사 담당자가 초안을 수정·검토하고 최종 승인한다. 변환 실패 시 지원하는 규칙 값을 수기로 입력하며 동일한 검토·승인 경로를 사용한다. LLM 방식이 구현되지 않으면 제외하고 수기 입력 경로를 유지한다. 이는 기존의 수기 입력 전용 결정을 변경한 2026-09-16 합의다.
3. 가입자는 해당 특약을 선택하고 **두 번 이상의 모의 운행**을 순차적으로 처리한다. 실제 GPS, 외부 내비게이션 및 실제 보험사 시스템 연동은 데모 범위 밖이다.
4. 승인된 규칙으로 계산된 누적 결과와 이전·신규 상태의 관계를 Midnight에서 검증 가능하게 만드는 것이 핵심 기술 목표다.
5. 가입자는 보험사에 제공할 결과를 확인하고 제출을 승인한다. 보험사 화면과 API에는 원본 위치, 구간별 속도, 정확한 운행시각, 이동경로를 제공하지 않는다.
6. 보험사는 검증 결과와 특약 판단에 필요한 최소한의 결과를 보고 할인 여부를 결정한다. `증명 유효`와 `할인 승인`은 별개 상태다.
7. 계산 결과, 승인 규칙 또는 이전 상태를 바꾼 경우 유효하지 않은 계산임을 실제 검증 경로에서 확인하는 것을 목표로 한다.
8. 제출된 운행기록과 계산 입력을 연결하기 위해 **Dataset Merkle Tree를 MVP에 적용**한다. 실제 기록 생성이나 전체 운행의 완전성을 보장하는 구조로 표현하지 않는다.
9. 원본 주행데이터는 **Midnight 검증과 트랜잭션 반영 확인 후 삭제**한다. 증명 생성 또는 트랜잭션 처리 실패 시 재처리를 위해 보관한다. 구체적인 보관·재처리·삭제 구현은 미정이다.
10. 동일 평가 결과의 중복 제출은 Backend 검사와 온체인 `nullifier`로 방지한다. 미적용 후 점수가 바뀐 경우의 재신청은 아직 제안 단계다.
11. 이메일 기반 로그인과 역할 구분을 MVP에 포함한다. Supabase Auth의 이메일·비밀번호 방식을 채택했다. 객체별 권한은 Backend에서 검사하며 정확한 API 계약은 구체화 전이다.
12. 가입자는 Midnight Wallet SDK를 사용하는 자체 보관형 임베디드 월렛으로 제출과 트랜잭션을 승인한다. 가입자 개인키를 Drivacy 서버에 저장하지 않으며 보험사는 별도 월렛 설치 없이 서비스를 이용한다. 다중 기기 월렛 복구는 MVP에서 제외한다.
13. 최신 개발 지시에 따라 운행별 후보 상태는 Midnight에서 최신 이전 상태와 상태 전이를 검증하고 체인 반영이 확인된 후에만 확정한다. 미확정·실패 시 이전 확정 상태를 유지한다. DB를 먼저 확정하고 검증 실패 시 되돌리는 구조를 사용하지 않는다.
14. Frontend는 React를 사용하며 디자인 완성 후 구현한다. 현재는 프로토타입을 구현 판단 대상으로 삼지 않고, 먼저 GitHub 클론 환경에서의 LLM 실행 조건을 확인한다. Backend는 Node.js + TypeScript + Express로 선정했다. Frontend는 React + TypeScript + Vite를 사용하고 DB·인증·배포 등은 docs/TECH_STACK.md를 따른다. 선정과 구현·검증 완료는 구분한다.

## 개발 착수 기준과 최초 점검 — 2026-09-16

대상은 **2026 미드나잇 해커톤**이다. 문서 우선순위는 최신 사용자 명시적 지시 → 최종 API 명세서·데이터 플로우 → 최종 기획 문서 → 기존 코드·주석 → 일반 개발 관행이다. 문서에서 제안·미정으로 표시한 내용을 임의로 확정하지 않는다. 구현을 막지 않는 임시값은 확정 사업 규칙과 분리하여 표시한다.

최신 지시는 이메일 로그인, 자체 보관형 임베디드 월렛과 체인 검증 후 상태 확정을 MVP 기준으로 지정했다. 따라서 기존 문서의 월렛 미확정 표현과 아래의 과거 신청 시 일괄 증명 제안보다 이 기준이 우선한다. 별도 변경 지시 전에는 운행별 검증·확정과 최종 확정 상태를 이용한 신청 흐름을 따른다.

확인한 자료는 다음 세 개다.

- [사업계획서](https://docs.google.com/document/d/1y26KcwcMx-VlILhsubqiB-TQxbYpC9wq4IacrpMOe0g/edit): 제품 목적, 참여 주체, MVP와 고도화 범위.
- [Backend 통합 흐름 기술 설계](https://docs.google.com/document/d/1zbBt5njzajgOpBJQZQ0E6d2jH2V8EnLA8waYlNoOHqs/edit): 운행별 상태 검증·확정, 최종 신청, 데이터 보관·공개 및 검증 책임.
- [개발 범위·화면·API 시트](https://docs.google.com/spreadsheets/d/1F24KnjbSbAjASHNdG83hZBqFFhPUJKLE-PwJTJ703RU/edit): 기능 ID, 화면별 행동과 Backend 처리, API 경로 제안. 시트 52행은 URI·상태코드를 구현 제안으로 표시하고 55행은 인증 API, 87행은 구체 응답 스키마·오류코드가 미정임을 명시한다. 현재 제공본을 완결된 외부 API 계약으로 취급하지 않는다.

최초 점검 당시 로컬 `feat` 체크아웃에는 `AGENTS.md`, `CLAUDE.md`, `PROJECT_DIRECTION.md`, `README.md`만 있었다. 후속 문서화에서 `docs/`를 추가했으며 앱·HTML 소스, 패키지 설정, 환경변수 예시, DB 스키마, 테스트와 배포 설정은 아직 없다. 시트 110행의 저장소 내 HTML 프로토타입 설명은 최초 점검에서 확인되지 않았으며, 공유된 와이어프레임 사이트와 구분한다.

후속 위임으로 Frontend·Backend·DB·인증 기술 스택은 선정 완료다. 첫 수직 기능의 정확한 API 계약은 구현 전 구체화한다. 첫 기능은 이메일 로그인·동의 후 본인 모의 보험계약을 조회해 화면에 표시하는 흐름을 제안한다. 시트의 `GET /api/v1/contracts?query={query}`를 기준 후보로 사용하되 요청·응답 스키마와 오류 규약을 임의 생성하지 않는다. Rule/State 스키마, 회로 입력·공개 범위, 네트워크·SDK 조합, 운행별 트랜잭션 승인 상세와 재신청 정책은 해당 기능 구현 전에 확정한다.

후속 사용자 확인으로 Frontend React는 정해졌으며 화면 구현은 디자인 완성 후 착수한다. 위 첫 화면 포함 수직 기능은 이후 구현 제안으로 남긴다. Backend·DB·인증은 TECH_STACK의 조합으로 선정했다. 외부 API 계약은 구체화 전이다. 임베디드 월렛의 Midnight Wallet SDK 사용은 사업계획서에도 명시돼 있으므로 미확정인 것은 사용 여부가 아니라 구체적인 SDK 버전·승인 구현이다. 네트워크는 로컬 확인 후 Preprod를 사용한다.

### 클론 환경에서의 LLM 확인 — 2026-09-16

공개 저장소를 별도 임시 디렉터리에 새로 클론했다. 기본 브랜치는 `main`, 확인 커밋은 `4445e9a67eaee6ba0e1c3e53cc52e82a83f11234`이며 문서 4개만 있다. LLM 호출 코드, 패키지 설정과 환경변수 예시가 없으므로 현재 제출 코드에서 LLM이 실행된다고 주장하지 않는다.

Gemini 공식 모델 목록 REST API를 API 키 없이 호출한 결과 HTTP `403 PERMISSION_DENIED`로 인증을 요구했다. 이 확인은 모델 목록 조회이며 생성 요청·규칙 추출 성공·비용·성능을 검증한 것은 아니다. REST 사용은 SDK 의존성을 줄일 수 있지만 인증·모델 접근·사용량 제한을 없애지 않는다.

후속 합의로 **클론한 앱 → 팀 배포 Backend → LLM API**를 기본 실행 방식으로 확정했다. 심사위원은 LLM 키를 입력하지 않는다. 레포에는 서버의 공개 주소를 기본 설정으로 넣고 실제 키는 팀 서버의 환경변수 또는 Secret에 보관한다. 모델·프롬프트·규칙 출력 형식은 팀 Backend에서 관리한다. 공급자는 Gemini 안정판 Flash, 기본 모델은 TECH_STACK 기준이며 호스팅은 Cloud Run으로 선정했다. 실제 서버 주소는 배포 전이고, 서버 연결 코드와 실제 생성 호출은 아직 구현·검증하지 않았다. 인터넷과 운영 중인 팀 서버가 필요하다. 로컬 자체 키 방식은 선택 실행안이며 LLM 실패 시 수기 입력 경로를 유지한다. 자세한 결정과 제출 전 확인 기준은 [docs/LLM_EXECUTION.md](docs/LLM_EXECUTION.md)에 정리한다.

연결된 **Locate Google Sheets spreadsheet** 채팅의 사용자 지시·팀 답변과 확정·제안·미정 구분은 [docs/README.md](docs/README.md)에 정리했다. 무료 범위 활용은 검토 사항이며 공급자·호스팅·사용량을 선정하기 전에 총비용 0원으로 확정하지 않는다.

2026-09-16 사용자가 전달한 본사 리뷰·심사 안내에 따르면, 앞서 우려한 버전 불일치는 LLM 모델 버전이 아니라 컴파일 관련 문제였으며 이번 해커톤 심사에서는 Claude Code 리뷰 스킬을 사용해 컴파일 버전을 바꿔가며 확인할 예정이다. 이 안내를 작업 기준에 반영해 컴파일 버전 차이 자체를 심사 차단 우려나 추가 선행 결정사항으로 다루지 않는다. 심사위원의 LLM 키 입력을 없애는 팀 서버 방식은 유지한다. 이는 사용자가 전달한 안내 기록이다.

## 백엔드 명세 질의 답변 반영 — 2026-09-16

사용자가 전달한 팀 답변을 아래처럼 구분한다. 공유 문서의 기존 표현보다 이 대화에서 확인한 최신 합의를 기준으로 하되, 검토 요청이나 제안을 확정 사양으로 바꾸지 않는다.

| 항목 | 합의 수준 | 명세 작성 기준 |
| --- | --- | --- |
| 1. 평가 상태 | 처리 완료의 의미 확정 | `처리 완료`는 보험사의 할인 적용·미적용 결정까지 끝난 상태다. 제안된 접수 → 증명 생성/검증 중 → 검증 완료 → 보험사 판단 대기 → 적용/미적용 흐름을 바탕으로 하되, 세부 상태명과 실패·재처리 상태는 구현 설계에서 정한다. |
| 2. 보험사 제공 결과 | 구체적인 점수·거리 제공 방향, 최종 확정 전 | 최종 점수와 누적 거리를 정확한 숫자로 제공하는 쪽을 선호하지만 답변에 추가 고민이 명시됐다. 평가기간, 조건 충족 여부, 예상/적용 할인율, Rule Version, 검증 결과, 트랜잭션 ID를 포함한 최종 항목·정밀도와 공개 ledger/Public Input 범위는 별도로 확정한다. |
| 3. 점수 변화·계산 근거 | 제시한 항목과 원본 비공개 원칙 확인 | 이전·변경 점수, 증감, 운행 거리·소요시간, 과속·급가속·급제동 집계 횟수, 항목별 가점·감점, Rule Version을 설명 항목으로 다룬다. 가입자용 설명과 보험사에 제공할 계산 근거의 세부 범위는 구분한다. GPS 위치·이동경로·구간별 속도·정확한 운행시각은 제공하지 않는다. |
| 4. Rule 등록 | 확정 | 약관 업로드 → LLM 초안 → 보험사 수정·검토 → 최종 승인 → Rule Hash 생성 → Midnight 등록. LLM 실패 시 수기 입력 후 같은 승인 경로를 사용한다. 내부 승인과 체인 등록 완료는 구분한다. |
| 5. 증명 시점 | 과거 신청 시 일괄 증명 선호·검토 요청 | 당시 일괄 증명안을 검토했으나 이후 최신 개발 지시는 운행별 Midnight 검증·체인 반영 후 상태 확정을 요구한다. 현재 구현 기준은 최신 지시와 데이터 플로우다. |
| 6. Merkle Tree | Dataset 적용 확정, Driver 필수 채택 아님 | Dataset Merkle Tree를 넣는다. 사용자 신원 자체를 숨기는 서비스는 아니라는 답변에 따라 Driver Merkle Tree를 필수 사양으로 확정하지 않는다. 신청자·계약·특약의 자격 연결 방식은 별도 설계한다. |
| 7. 삭제·실패 | 확정, 세부 운영 미정 | 검증 및 트랜잭션 반영 확인 후 원본 삭제. 실패 시 원본을 보관하고 재처리한다. 신청 전 보관기간, 재시도 한도와 최종 실패·미신청 시 정리 기준은 미정이다. |
| 8. 중복·재신청 | 중복 방지 확정, 재신청 제안 | Backend와 `nullifier`로 동일 평가 결과의 중복 등록을 막는다. 미적용 후 점수 변화에 따른 재신청 허용 여부, 기존 신청으로 이동/차단하는 UX와 새 평가 결과의 식별 범위는 미정이다. |
| 9. 데모 수치 | 임시값 사용, 최종 규칙 미정 | 초기 85점·운행 후 87점, 332.4km·524.8km, 최소 500km, 10%·최대 12%는 임시 예시다. 레퍼런스 검토 후 산식·신청 거리·점수별 할인 구간을 정하고 화면을 통일한다. |

### 과거 신청 시 일괄 증명안의 검토 기준 — 현재 구현 기준 아님

한 번의 최종 증명으로 여러 운행의 계산과 누적 관계를 검증하는 설계를 검토할 수 있다. 이를 위해 Dataset Root에 묶인 기록이 실제 계산 입력과 연결되고, 승인 Rule 및 시작 상태에서 최종 누적 상태에 이르는 계산 관계가 회로로 제약되어야 한다. Root만 등록하거나 Backend의 최종 점수만 입력하는 것으로 기존의 누적 상태 검증 목표를 충족했다고 보지 않는다. 구체적인 회로 구성과 처리 가능한 운행 수는 아직 구현·성능 검증 전이다.

이 안을 채택하면 운행 종료 시 DB에 저장한 점수와 상태는 최종 증명 전의 계산 결과다. 매 운행마다 Midnight에서 검증·확정됐다고 표시하지 않는다. 운행별 체인 상태 갱신을 보여주는 데모 목표를 유지할지, 최종 증명 안에서 두 운행의 누적 계산을 검증할지는 증명 시점 결정과 함께 정한다.

원본을 최종 검증·트랜잭션 반영 후 삭제한다는 합의와 결합하면, 일괄 증명에 필요한 기록은 운행 종료 후 신청·검증 완료까지 보관할 필요가 있다. 보관 기간이 운행 처리 시간만으로 끝난다고 안내하지 않으며, 신청하지 않은 기록과 최종 실패 기록의 정리 기준은 별도 확정한다.

### 검증의 한계

영지식증명은 **제출된 기록에 대해 승인 규칙과 상태 전이 관계가 성립하는지**를 다룬다. 기록이 실제 운행에서 나왔는지, 빠진 운행이 없는지, 모든 운행이 제출됐는지, 원본이 실제 삭제됐는지, 보험사가 받은 결과를 재사용하지 않는지까지 증명하지 않는다. Compact의 비공개 입력이나 witness가 진짜 운행 데이터라는 보장도 없다.

현재 설계에서 원본은 Drivacy의 계산·증명 처리 영역에 일시적으로 존재할 수 있다. 비공개 대상은 보험사와 공개 원장이며, 플랫폼 자체에도 원본이 보이지 않는다고 주장하지 않는다.

## 발표용 데모 흐름 — 제안, 세부값 미확정

아래는 팀의 발표와 구현 논의를 위한 권장 시나리오다. 수치와 화면 구성은 아직 확정 사양이 아니다.

1. **보험사 화면:** 예시 약관을 업로드해 LLM 초안을 만들고 담당자가 수정·검토해 버전 `v1`으로 승인한다. 변환 실패 시 지원되는 규칙값을 수기로 입력한다. 승인본의 Rule Hash 생성과 Midnight 등록 상태를 구분한다.
2. **가입자 화면:** 특약을 고른 뒤 첫 번째 모의 운행을 처리한다. 점수는 통과하지만 최소 누적 거리가 부족한 상태를 보여준다.
3. **가입자 화면:** 두 번째 운행을 처리해 기존 상태에 연결한다. 누적 거리와 점수가 함께 기준을 만족한다.
4. **제출 확인:** 가입자가 보험사·특약·평가기간과 공개될 결과를 확인하고 신청한다.
5. **보험사 화면:** 원본 없이 규칙 버전, 조건 충족 여부와 실제 Midnight 검증 결과를 확인한 뒤 할인 승인한다. 가입자에게 결정 결과를 보여준다.
6. **발표용 검증:** 정상 요청은 성공하고, 점수·규칙·이전 상태를 조작한 요청은 증명 생성 또는 검증에서 거부됨을 보여준다. 정상적으로 계산된 `할인 조건 미충족`은 **유효한 증명**일 수 있으며, `증명 무효`와 구별한다.

이전 논의에 사용한 **가상 예시**는 최소 100km, 80점 이상, 10% 할인, 과속 1회당 2점 감점, 급제동 1회당 3점 감점이다. 첫 운행 60km·과속 2회·급제동 1회라면 93점, 두 번째 50km·과속 1회·급제동 1회까지 누적하면 110km·88점이 된다. 이는 현재 화면의 임시 500km 시나리오와 별개인 과거 예시이며, 실제 보험상품이나 최종 산식으로 인용하지 않는다. 최종 데모 수치는 레퍼런스 검토 후 통일한다.

## 구현 우선순위와 도구

### 기능 분담과 작업 조건 — 2026-09-16

사용자는 새 브랜치 생성을 금지하고, 프론트 구현을 우선 제외한 기능 분담과 공통 작업 원칙 문서화를 요청했다. 개발자는 프론트+백엔드 1명과 백엔드 위주 2명으로 총 3명이라고 정정했다. Core·Agent·Midnight를 포함한 기능을 세 개발자에게 배정한다. 이번 작업은 기존 `feat`에서 문서만 작성하며 브랜치 생성·전환을 하지 않는다.

[docs/TEAM_WORKING_PLAN.md](docs/TEAM_WORKING_PLAN.md)에 A는 약관·LLM 규칙 초안, B는 인증·계약·규칙 승인·운행 API·DB 확정·원본 운영·신청·보험사 판단·서버 배포, C는 Rule Hash·Core 계산·Merkle·Compact·Midnight 검증·nullifier를 맡는 기능 분담을 작성했고 사용자의 추천안 채택 지시로 개발 기준으로 확정했다. A의 향후 프론트 작업을 고려해 Backend 범위를 좁혔다. 월렛 SDK와 승인 인터페이스는 C, 향후 프론트 연결은 A가 담당하는 안이다. B는 C의 실제 체인 반영 확인 결과를 받은 뒤 DB 상태를 확정한다. 기술 스택과 공통 작업 원칙도 사용자의 위임으로 채택했다. 실명 배정과 API/Rule/State 계약·보험 규칙·사업 정책의 세부 사항은 구체화 전이다. 선정 기준은 [docs/TECH_STACK.md](docs/TECH_STACK.md)에 기록한다.

### 유사 사례 참고 목적과 개발 도구 역할 — 2026-09-16 합의

- `midnight-awesome-dapps` 비교의 목적은 아이디어의 독창성·우열 평가가 아니라, Drivacy와 구조가 비슷한 구현에서 **Dataset 커밋먼트, Merkle Tree, 입력 연결과 상태 전이 검증 패턴**을 찾아 참고하는 것이다. 기존 소스의 적용 가능성을 확인하되, 구체적인 leaf·해시·직렬화·솔트·경로·회로 사양과 코드 채택은 아직 확정하지 않았다.
- 사용자는 Codex에 **MIDSKILLS 플러그인을 설치할 예정**이며, 설치 후 해당 개발 지원 기능을 활용하는 방향이다. 이번 합의는 설치 완료·호환성 검증을 의미하지 않는다.
- **Claude Code를 호출할 때는 사용자가 ‘미드나잇 엘리펀트’라고 부르는 개발 지원 도구를 사용한다.** 기존 조사에서 확인한 이름은 Midnight Expert이며, 두 이름의 동일성은 아직 확인되지 않았다. 실제 호출 전에 대상 도구를 확인하며, 이번 합의만으로 Claude Code나 해당 도구를 호출한 것으로 기록하지 않는다. Midnight 도구의 현재 세션 활성화 제한도 유지한다.

팀은 **Claude Code의 Midnight Expert를 활용해 초반 개발 틀을 만들 계획**이다. 그 도구를 사용했다는 사실만으로 Midnight 기능이 구현·검증된 것으로 보지 않는다. 생성된 Compact 코드, 클라이언트, 네트워크 연동을 실제로 컴파일하고 정상·실패 케이스를 확인해야 한다.

2026-09-16 후속 지시에서도 Claude와 Midnight 개발 지원 도구를 함께 사용할 계획을 확인했다. 사용자가 ‘엘리펀트’라고 언급한 도구와의 동일성은 미확인이며 GitHub에서 확인한 이름은 Midnight Expert다. 오픈소스 후보는 `create-mn-app`, `example-bboard`, 공식 `midnight-wallet`, `midnight-local-dev`, OpenZeppelin `compact-contracts`이며, [docs/MIDNIGHT_OPEN_SOURCE.md](docs/MIDNIGHT_OPEN_SOURCE.md)에 용도·라이선스·조사 커밋을 기록했다. 후보 조사는 설치·도입 확정이나 호환성 검증이 아니다. Midnight Expert README에 따라 Windows에서는 WSL2 개발 경로를 추천한다.

추가 요청으로 ZK의 Compact 표준 라이브러리·Merkle 테스트·nullifier 예제·ZKLoan 조건 평가와 `midnight-zk`를 조사했다. 약관 처리·규칙 입력·검증·테스트에는 PDF.js, Zod, React Hook Form, Vitest를 추천 후보로 함께 기록했다. 기존 기능 구현에 필요한 소스·라이브러리 재사용을 검토하는 방향이며, 패키지 설치·버전 채택·PDF 파일 규격·인증/DB 서비스 선정은 이번 조사에서 확정하지 않았다.

규칙 등록은 LLM 초안 생성을 우선 시도하고 수기 입력 경로를 유지한다. 전체 구현은 기술 스택·첫 API 계약 확정 → 이메일 로그인·동의·본인 모의 계약 조회 → Rule 초안 생성·검토·승인 → 임베디드 월렛과 실제 증명 경로 연결 → Dataset Merkle Tree 및 두 운행의 체인 검증 후 누적 상태 확정 → 최종 신청·보험사 결정 → 재현 가능한 클론·실행 안내를 권장한다. 가입자 임베디드 월렛은 MVP에 포함하며, SDK의 구체 버전은 실제 연결 시 고정하고 네트워크는 로컬 검증 후 Preprod를 사용한다. 실제 보험사 REST 연동은 MVP 범위 밖이다.

## 미해결 질문

- LLM 초안과 수기 입력이 공통으로 사용하는 규칙의 정확한 필드·자료형·허용 범위는 무엇인가?
- 원본을 비공개 입력으로 사용하는 Compact 회로에서 계산 및 상태 전이를 어디까지 제약할 것인가?
- 운행별 상태 전이와 최종 신청을 어떤 회로 구조로 구성하고 Dataset Root를 어떻게 연결할 것인가?
- 보험사에 제공할 점수·거리의 정밀도와 최종 항목, 가입자용 설명과 보험사용 계산 근거의 차이, 공개 ledger/Public Input 범위는 무엇인가?
- 원본의 보관 주체·접근권한, 재시도 한도, 미신청·최종 실패 시 정리 정책과 삭제 확인 방법은 무엇인가?
- 미적용 후 점수 변화에 따른 재신청을 허용할 것인가? 허용한다면 신청 버전과 `nullifier`의 식별 범위를 어떻게 정할 것인가?
- Dataset Merkle Tree의 기록 단위·순서·솔트 보관과 신청자 자격 연결 방식은 무엇인가?
- 최종 데모의 산식·거리·점수별 할인 구간은 무엇인가?
- 지갑 및 로컬/테스트넷의 실제 재현 방법은 무엇인가?
- 실제 보험사가 외부 계산 결과를 독립 검증하기 위해 비용을 낼 필요가 있는지 확인할 수 있는가?

## 대화에서 방향이 바뀌면

결정된 내용을 이 문서의 해당 섹션에 반영하고 날짜를 갱신한다. 확정·제안·미결정을 구별하며, 기존 결정과 충돌하면 최신 결정과 그 이유를 기록한다. 실행 방법, 구현 상태, 제출 설명이 달라졌다면 README도 함께 갱신한다. 개인 대화나 민감정보는 그대로 복사하지 않고 프로젝트에 필요한 결정만 남긴다.
