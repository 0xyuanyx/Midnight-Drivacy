# 신규 가입·월렛 화면 검수

2026-09-25. 범위: React Native 프론트와 Expo web 미리보기. 실제 인증·월렛 구현 완료 아님.

## 화면별 대조

Figma 파일 KYGMIgTmnnrQVEDMy338xE, Page 1의 아래 프레임을 직접 확인했다.

| 화면 / node | 반영 내용 | 의도적 차이 |
|---|---|---|
| 이메일 / 88:5966 | 중앙 헤더, 2줄 제목, 이메일 필드, 고정 CTA | 발송 연결 전 안내 |
| 6자리 코드 / 88:5978 | 6칸 입력, 붙여넣기, 재전송 타이머, 이메일 변경 | 실제 발송 주장 대신 확인용 코드 안내 |
| 가입 정보 / 88:6000 | 이름·생년월일·휴대전화, 정보 카드 | 기존 카드에서 처리 안내 확장·확인; 서버 저장 없음 |
| 월렛 안내 / 88:6022 | 진행 eyebrow, 세 가지 색상 안내 카드 | ‘내 정보’ 대신 ‘월렛 개인키’로 보관 범위 명확화 |
| 월렛 암호 / 88:6046 | 암호·확인, 12자 조건, 기기 분실 체크 | 체크 기본 해제, 불일치·미확인 오류 |
| 연결 중 / 88:6065 | 회전 링, 고정 중앙 점, 상태 카드 | 미연결 서비스의 성공 상태를 표시하지 않음 |
| 연결 완료 / 88:6086 | 중앙 상태 아이콘·제목·설명, 월렛 정보 카드, CTA | 기본 실행은 중립 아이콘과 ‘연결 준비’, 주소/성공 체크 없음 |

## 비교 → 수정 → 재검증

- 데스크톱의 402×874 휴대폰 프레임과 360px 브라우저 폭에서 검토했다. 360px에서는 외곽 휴대폰 여백을 제외한 실제 콘텐츠가 더 좁으므로 보수적 검수다.
- 공통 CTA 크기 54/radius 27, 좌우 20, 역할별 Pretendard, withBack eyebrow 간격 24를 유지했다. Figma의 더 작은 보조 글씨·다른 CTA 하단 간격을 그대로 복사하지 않았다.
- 이메일/코드/가입 정보에는 eyebrow 공간을 예약해 검정 제목 시작점을 월렛 화면들과 정렬했다.
- 새 화면만 웹 상단 여백을 추가했던 초안을 제거하고 기존 AppScreen의 18px로 통일했다. Figma 상태바 영역과 웹 휴대폰 외곽은 동일한 구현물이 아니므로 원본 좌표를 강제로 복사하지 않았다.
- 재전송 초기 표기의 `00:60`을 `01:00`으로 수정했다.
- 오류 안내를 고정 CTA 바로 위로 이동하고 배경을 적용해 긴 폼 아래에서 숨거나 겹치지 않도록 했다.
- 화면 단계 변경 시 스크롤 컨테이너를 초기화해 이전 입력 화면의 스크롤 위치가 다음 화면에 남지 않도록 했다.
- 좁은 화면에서 가입 정보 카드까지 스크롤 가능한 것과 CTA 접근을 확인했다.
- 완료 CTA가 시작 화면 재탐색 없이 보험조회 동의 시트를 여는 것을 브라우저에서 확인했다.
- 동작 테스트: 이메일/코드 오류, 실제 날짜 검증, 정보 안내 확인, 암호 불일치, 연결 실패/재시도 시 중복 생성 방지, 이메일 중복 요청 차단, 이전 화면으로 돌아간 뒤 늦은 응답 무시, 코드 만료, 민감 필드 저장 배제, 초기화·라우트 보호.

## 별도 디자인 요청 없이 처리한 항목

- 오류는 필드가 있는 동일 화면의 CTA 위 안내로 처리.
- 재전송은 코드 화면의 기존 보조 버튼 사용.
- 연결 실패는 진행 화면의 상태 카드와 재시도 CTA 사용.
- 개인정보 안내는 가입 정보의 기존 카드 확장 사용. 현재 문구는 로컬 화면 확인 범위의 안내이며 운영용 약관이 아니다.

## 실제 서비스 연결 전 필요한 결정·요청

1. B: 이메일 OTP 설정·유효시간·재전송 제한·Supabase 세션과 신규 DRIVER provisioning 계약. 현재 60초/5분은 화면 동작 기준일 뿐 서버 인증 보안 정책이 아니다.
2. B: 이름/생년월일/전화번호의 수집 필요성, 저장 API, 수집 목적·보유 기간의 승인된 문구. 현재 메모리 폼만 있고 서버 전송 없음.
3. C: 기기 월렛 생성/보관/잠금 해제/연결 상태 API. 현재 주입 가능한 화면 서비스 인터페이스는 실제 Wallet SDK 구현이 아니다.
4. A+B+C: 재로그인·같은 기기·새 기기를 판별하고 잠금 해제 또는 복구 미지원 안내로 분기. 기존 암호 필드/카드를 재사용할 수 있어 신규 디자인 요청은 필수 아님.
5. C: 생성 중 중단·오류와 이미 생성된 월렛 재연결의 복구 계약. 현재 화면 내 재시도는 같은 핸들을 재사용하지만 앱 종료 후 실제 키 복구는 SDK 연결 범위다.

현재 추가 디자이너 요청은 없음. iOS/Android 실제 키보드·보안 저장소·이메일 딥링크·네트워크 검증은 수행하지 않았으며 브라우저 검수를 실기기 검증으로 간주하지 않는다.

## 실행 검증 결과

### 2026-09-25 입력 UX 후속 보정

- 첫 화면 CTA 36px 상향. 로고 파일은 기존 자산을 유지하며 실제 브라우저에서 표시를 확인했고, 작은 화면에서는 여백/높이를 축소한다.
- 이메일은 잘못된 값으로 포커스를 벗어나면 입력 아래 오류를 표시한다. 수정 즉시 원래 회색 안내를 복원하며 올바른 형식일 때만 발송 CTA가 활성화된다.
- 코드 6자리와 유효시간, 가입 정보 유효성·확인 체크, 암호 조건·확인 일치·분실 확인에 따라 단계별 CTA를 제어한다.
- 날짜/전화번호 붙여넣기와 직접 입력에 자동 구분자를 적용한다.
- 암호 조건 충족 및 확인 일치는 초록 안내로 표시한다. 암호를 다시 바꿔 조건이 깨지면 안내와 CTA가 즉시 바뀐다.
- 홈 고정 이름은 중립 문구로 교체. 실제 계정 이름은 가입 프로필 API 연결 후 표시할 계획이며 현재 연결된 것처럼 주장하지 않는다.

- 모바일 Jest: 10개 suite / 70개 테스트 통과.
- 저장소 기본 테스트: Shared 34 + Rule Draft 38 + Backend 91 = 163개 통과. 기존 PDF fixture 경고는 남아 있다.
- 모바일 TypeScript, 변경 파일 ESLint, `git diff --check` 통과.
- Expo web export: 19개 정적 경로 생성 성공. NO_COLOR/FORCE_COLOR 환경 경고는 빌드 실패가 아니다.
- 실제 이메일·Supabase·Wallet SDK 요청, iOS/Android 실기기 테스트, 커밋·푸시는 이번 검증에 포함하지 않았다.
